import { ArrowLeft } from "lucide-react"; import type { ReactNode } from "react";
export const Container=({children,className=""}:{children:ReactNode,className?:string})=><div className={`mx-auto w-full max-w-[1320px] px-5 sm:px-8 lg:px-12 ${className}`}>{children}</div>;
export const Section=({children,id,className=""}:{children:ReactNode,id?:string,className?:string})=><section id={id} className={`scroll-mt-20 py-24 lg:py-40 ${className}`}>{children}</section>;
export function SectionHead({eyebrow,title,text,light=false}:{eyebrow:string,title:ReactNode,text?:string,light?:boolean}){return <div className="mb-12 max-w-3xl lg:mb-16"><p className={`mb-5 tracking-[.18em] ${light?"text-mist":"text-signal"}`}>{eyebrow}</p><h2 className={`display text-balance text-4xl font-bold leading-[1.08] lg:text-6xl ${light?"text-cloud":"text-ink"}`}>{title}</h2>{text&&<p className={`mt-6 max-w-2xl text-lg leading-8 ${light?"text-mist":"text-ink/65"}`}>{text}</p>}</div>}
export function Button({href,children,secondary=false,className=""}:{href:string,children:ReactNode,secondary?:boolean,className?:string}){return <a href={href} target={href.startsWith("http")?"_blank":undefined} rel={href.startsWith("http")?"noreferrer":undefined} className={`group inline-flex min-h-12 items-center justify-center gap-3 rounded-full px-6 py-3 text-sm font-bold transition duration-200 hover:-translate-y-0.5 ${secondary?"border border-cloud/20 text-cloud hover:border-signal hover:bg-cloud/5":"bg-cloud text-void shadow-glow hover:bg-signal"} ${className}`}>{children}<ArrowLeft className="h-4 w-4 transition-transform group-hover:-translate-x-1"/></a>}
